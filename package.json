{
  "name": "neutts-mcp-server",
  "version": "0.1.0",
  "description": "MCP streaming HTTP server (Fastify) that exposes the NeuTTS Python backend as a voice-cloning text-to-speech tool. Accepts a reference audio sample in any format and a piece of text, normalises the audio to WAV via ffmpeg, runs NeuTTS for synthesis and returns the result as MP3.",
  "type": "module",
  "main": "src/server.js",
  "scripts": {
    "start": "node src/server.js",
    "dev": "node --watch src/server.js"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "@fastify/sensible": "^6.0.1",
    "@modelcontextprotocol/sdk": "^1.17.0",
    "fastify": "^5.2.1",
    "zod": "^3.24.1"
  }
}
